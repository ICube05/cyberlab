{
  "name": "@cyberlab/lab-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The CyberLab lab engine: pluggable runtimes plus a set of genuinely vulnerable target applications that execute for real inside a sandbox.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "dependencies": {
    "@cyberlab/core": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
