{
  "name": "@cyberlab/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Domain model, mastery maths and adaptive planner for CyberLab. No I/O, no framework, no Node built-ins — safe to import from both the server and the browser.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
