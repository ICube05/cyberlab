{
  "name": "@cyberlab/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The CyberLab tutor: a provider-agnostic AI layer (Ollama, Gemini) with a deterministic offline fallback, mode-aware prompting and structured lesson context.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "dependencies": {
    "@cyberlab/core": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
